export type TDefect = {
    ID: number;
    DESCRICAO: string;
};

export type TProductRequest = {
    ID: number;
    DESCRICAO: string;
    REFERENCIA: string;
};
  

export type TOptionUnitRequest = {
    DESCRICAO: string;
    ID: number;
};

export type TOptionLineRequest = {
    DESCRICAO: string;
    ID: number;
};